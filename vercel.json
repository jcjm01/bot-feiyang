{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/leads", "dest": "/api/leads.js" },
    { "src": "/api/webhook", "dest": "/api/webhook.js" }
  ]
}
